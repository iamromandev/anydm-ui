"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const jsxRuntime = require("@builder.io/qwik/jsx-runtime");
const qwik = require("@builder.io/qwik");
const popoverTypes = require("./popover-types.qwik.cjs");
const HPopoverRoot = qwik.component$((props) => {
  const { id, "bind:anchor": givenAnchorRef, "bind:panel": givenPanelRef, floating = true, manual, hover = false, gutter, flip = true, shift, hide, arrow, ancestorScroll = true, ancestorResize = true, elementResize = true, animationFrame = false, transform, strategy, ...rest } = props;
  const anchorRef = givenAnchorRef;
  const rootRef = qwik.useSignal();
  const defaultPanelRef = qwik.useSignal();
  const panelRef = givenPanelRef ?? defaultPanelRef;
  const triggerRef = qwik.useSignal();
  const arrowRef = qwik.useSignal();
  const isOpenSig = qwik.useSignal(false);
  const localId = qwik.useId();
  const compId = id ?? localId;
  const rootId = `${compId}-root`;
  const context = {
    anchorRef,
    compId,
    floating,
    hover,
    panelRef,
    triggerRef,
    arrowRef,
    isOpenSig,
    localId,
    manual,
    arrow,
    gutter,
    flip,
    shift,
    hide,
    strategy,
    ancestorScroll,
    ancestorResize,
    elementResize,
    animationFrame,
    transform
  };
  qwik.useContextProvider(popoverTypes.popoverContextId, context);
  return /* @__PURE__ */ jsxRuntime.jsx("div", {
    ref: rootRef,
    id: rootId,
    ...rest,
    children: /* @__PURE__ */ jsxRuntime.jsx(qwik.Slot, {})
  });
});
exports.HPopoverRoot = HPopoverRoot;
