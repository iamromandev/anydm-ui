import { PropsOf, Signal } from '@builder.io/qwik';
import { Floating, Placement } from './popover-types';
export type PopoverRootProps = {
    popover?: 'manual' | 'auto';
    manual?: boolean;
    ref?: Signal<HTMLElement | undefined>;
    floating?: boolean | Placement;
    /** @deprecated Use the tooltip instead, which adheres to the WAI-ARIA design pattern. */
    hover?: boolean;
    id?: string;
    'bind:anchor'?: Signal<HTMLElement | undefined>;
    'bind:panel'?: Signal<HTMLElement | undefined>;
};
export type PopoverProps = PopoverRootProps & {
    floating?: boolean | Placement;
} & Floating & PropsOf<'div'>;
export declare const HPopoverRoot: import('@builder.io/qwik').Component<PopoverProps>;
